few-shot CoT	Apart from the classification task, please state the Chain of thought or the reasoning behind your classification. In such sense, your answer should be:

{

    'is_promise': 1,
    'is_efficiency': 1,
    'is_ethics': 0,
    'reason': '<here you write the reasons behind your decisions>'

}

The following are some examples based on real responses of participants:

The message is:
“I will choose roll on all the desicions, in order for both of us to have the chance to get more money if you are interested.”

The classification should be:
{
    'is_promise': 1,
    'is_efficiency': 1,
    'is_ethics': 0
    'reason': ' The message explicitly commits to choosing ROLL, which constitutes a promise. It also highlights that both players can obtain more money, which is an appeal to efficiency (i.e., maximizing joint payoff). There is no moral or equity-based argument, so it does not appeal to fairness.'
}


The message is:
“Let's make this fair. I guess we BOTH want some money.”

The classification should be:
{
    'is_promise': 0,
    'is_efficiency': 0,
    'is_ethics': 1
    'reason': 'The message emphasizes fairness (“make this fair”), which classifies it as an appeal to fairness. It does not contain a commitment to a future action, so it is not a promise, and it does not argue that cooperation maximizes total surplus, so there is no efficiency appeal.'
}